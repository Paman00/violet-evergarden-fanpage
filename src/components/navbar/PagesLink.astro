---
interface Props {
    url: string;
}
const { url, ...rest } = Astro.props;
---

<li {...rest}>
    <a
        class="block w-full cursor-pointer border-b border-golden py-3 font-normal transition hover:text-beige-light md:border-none md:py-0"
        href={url}>
        <slot />
    </a>
</li>
